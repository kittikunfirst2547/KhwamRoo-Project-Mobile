name: khwamroo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  firebase_core: ^4.7.0       # ← แก้
  firebase_auth: ^6.4.0       # ← แก้
  cloud_firestore: ^6.1.3
  firebase_storage: ^13.3.0 # ← แก้
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  flutter:
    sdk: flutter





dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/



